<section class="container">
  <h1>Animes</h1>
  <hr />

  <div class="row">

    {{#each animes}}
      <div class="col-md-2">
        <article class="character">
          <h2>{{name}} <small>#{{mal_id}}</small></h2>
          <hr />
          <img src="{{images.jpg.large_image_url}}" alt="" />
          <p><strong>Title:</strong> {{title}}</p>
          <p>Episodes: {{episodes}}</p>
          <p>Status: {{status}}</p>
          {{!-- <iframe src="{{trailer.embed_url}}" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen="" title="video" style="width: 50%; height: 450px;"></iframe>  --}}
          {{! <div class="btn-group" style="width: 100%"> }}

        </article>
       {{#if ../canEdit}}
  <a href="/animes/{{mal_id}}/edit">Edit</a>
{{/if}}
{{#if ../canDelete}}
  <form action="/animes/{{mal_id}}/delete" method="post">
    <button type="summit">🗑️</button>
  </form>
{{/if}} 
      </div>
    {{/each}}

    <br />
    <br />
    <button id="cargar-mas">Página siguiente</button>

  </div>

</section>